# CMWE — Conditional Mechanistic Weight Edits

Dynamic multi-adapter system for hallucination reduction in large language models.
Includes:
- `math_guard` and `citation_guard` LoRAs
- Risk-based detector
- Conditional router
- Evaluation + logging utilities

## Usage
```bash
python scripts/conditional_infer_multi.py

```
